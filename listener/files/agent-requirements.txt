# ovos-bus-client stays vendored: the listener needs a send lock, emit_checked
# and shared session stamping that upstream does not carry, and there is no
# thalovant fork to pin. The tree here is upstream 2.10.1a1 with
# OpenVoiceOS/ovos-bus-client#276 and those three changes re-applied.
ovos-bus-client @ file:///tmp/vendor/ovos-bus-client
# hivemind-ovos-agent-plugin no longer needs vendoring: it was only pinned to a
# git commit that upstream deleted, and 0.3.9a1 on PyPI supports the
# hivemind-bus-client 1.x line this image now runs.
hivemind-ovos-agent-plugin==0.3.10a1
