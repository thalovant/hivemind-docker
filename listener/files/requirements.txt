# Every hivemind package is installed STOCK from PyPI at its current alpha.
#
# These were git pins on thalovant forks. The forks existed to carry work that
# had not shipped yet; it has. Keeping them meant the base built a 4.x
# hivemind-core while the platform ran 5.x, so rebuilding this repo and rolling
# the result was a downgrade across a major version -- see
# infra-manifests build/hivemind-listener-v3-http-overlay/PROVENANCE.md.
#
# A pin only earns a fork if the behaviour is NOT in a release. Check with
# `pip download <pkg>==<latest>` and read the wheel before re-adding one.
poorman-handshake==2.0.3a1
hivemind-bus-client==1.2.3a1
hivemind-plugin-manager==0.10.0a1
hivemind-websocket-protocol==1.0.5a1
hivemind-core==5.3.0a1
hivemind-http-protocol==0.0.16a1
hivemind-mqtt-protocol==0.2.5a2
hivemind-audio-binary-protocol==2.2.2a1
hivemind-redis-database==0.1.1a8
# First-party and never published to PyPI, so this one stays a git pin and is
# the only reason the build still needs a token.
hivemind-redis-memory @ git+https://github.com/thalovant/hivemind-redis-memory.git@66647e908a9cc9f28efa4d9576f4f64b7b7f6361
hivemind-intent-quota-plugin[redis] @ file:///tmp/hivemind_intent_quota_plugin-0.1.0a4-py3-none-any.whl
setuptools
# The build-time assertion block imports packaging.version; it used to arrive
# transitively and no longer does, so a from-scratch build failed with
# ModuleNotFoundError regardless of any version change.
packaging
