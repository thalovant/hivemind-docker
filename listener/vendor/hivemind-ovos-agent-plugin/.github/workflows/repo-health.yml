name: Repo Health

on:
  pull_request:
    branches: [dev, master]
  workflow_dispatch:

jobs:
  repo_health:
    uses: OpenVoiceOS/gh-automations/.github/workflows/repo-health.yml@dev
    with:
      version_file: 'hivemind_ovos_agent_plugin/version.py'
